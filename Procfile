# define what applications to "start" when the application is running.
# this file is provided by paas-skeleton.git but is meant to be modified
# locally. Do not check changes into paas-skeleton.git unless absolutely 
# necessary
# 
# To start the applications defined here, you can use "honcho" (python)
# or "foreman" (ruby)
# 
# Example:
#   honcho -f Procfile start
# 
# Example django application using gunicorn:
#    web: django-admin.py run_gunicorn --config etc/gunicorn.py
